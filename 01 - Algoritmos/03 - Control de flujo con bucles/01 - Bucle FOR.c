#include <stdio.h>

int main() {
    int i;

    // Imprimir los números del 1 al 10
    for (i = 1; i <= 10; i++) {
        printf("Bucle recorrido por vez numero -> [%d]\n", i);
    }

    getchar();
    return 0;
}