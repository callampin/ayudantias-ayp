#include <stdio.h>

int main() {

    
    int numeros[5] = {1, 2, 3, 4, 5}; // Arreglo de 5 enteros
    for (int i = 0; i < 5; i++) {
        printf("Elemento %d: %d\n", i, numeros[i]);
    }


    getchar();
    return 0;
}